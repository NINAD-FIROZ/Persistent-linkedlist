# Persistent-linkedlist
A linked list which stores all the history of all changes happening to the linked list. Ie a new version of linked list is stored everytime a new node is added,deleted or updated. 
